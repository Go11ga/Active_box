/* Links shutdown */
(function(){

    let links = $("a");
    //console.log(links);

    for (let i = 5; i <= 5; i++) {
        links[i].addEventListener('click', function(event){
            event.preventDefault();
        });
    }

    for (let i = 18; i <= 18; i++) {
        links[i].addEventListener('click', function(event){
            event.preventDefault();
        });
    }

}());
